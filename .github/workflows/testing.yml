name: Testing 🧪

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  
  unit-test:
    name: Unit Testing
    
    uses: ./.github/workflows/build.yml
    with:
      test-env: UnitTests
  
  integration-test:
    name: Integration Testing
    timeout-minutes: 15

    uses: ./.github/workflows/build.yml
    with:
      test-env: UnitTests