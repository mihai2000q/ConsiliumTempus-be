name: Build 🔨

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest
    - name: Build
      uses: ./.github/workflows/build-action