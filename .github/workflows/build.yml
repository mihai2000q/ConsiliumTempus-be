name: Build 🔨

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest
    name: Building
    steps:
    - name: Checkout Repo
      uses: actions/checkout@v3

    - name: Build
      uses: ./.github/workflows/build-action